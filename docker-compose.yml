services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: aplexanalysis
      POSTGRES_USER: aplex
      POSTGRES_PASSWORD: aplex_dev
    ports:
      - "5432:5432"
    volumes:
      - aplex_pg:/var/lib/postgresql/data

volumes:
  aplex_pg:

